# 音楽理論学習プロジェクト

## 目的
音楽理論をゼロから体系的に学ぶための教材(台本+音声)を作成する。
自転車に乗りながら毎日30分「聴くだけ」で学習できる形式を目指す。

学習の狙い:
- ピアノ演奏を再開する際、理論的背景を理解した上で弾けるようになる
- 作曲の基礎知識を得る
- 音楽を聴く際の理解の深みを増す

## ユーザープロフィール(教材設計に反映すること)
- ピアノ経験あり。ベートーベン「悲愴」「月光」を演奏していたレベル。ここ5年はブランク、再開予定
- フルート経験 約1年
- 絶対音感あり → 具体的な音名(ド=C、嬰ヘ=F#など)を使った説明や、実際の音を鳴らす例が効果的
- 音楽理論の体系的学習は未経験。専門用語はゼロから丁寧に導入する
- 演奏経験はあるため、感覚的には理解が早い。理論用語と結びつける説明を意識する

## コンテンツ形式
- 1回10分前後、耳で聴くだけで完結する音声コンテンツ(自転車移動中を想定)
  - 当初は30分を想定したが、Day1試作(8.1分)を踏まえて10分前後を基準に変更(2026-08-11)
  - 全体の分量は変わらないため、30回よりカリキュラムの回数が増える可能性がある
- 楽譜や図に依存する説明はNG。音名・度数・言葉だけで完結させる
- Day1, Day2... と順番に積み上げる体系的カリキュラム形式(ゼロから徐々に発展)
- 各回の流れの目安(10分想定): 導入・前回の復習(1分) → 新しいトピック(6分、実音例を交える) → まとめ・次回予告(1分) → 簡単な聴き取り練習(2分)

## ディレクトリ構成
```
音楽理論学習/
  CLAUDE.md
  curriculum.md          # 全体のカリキュラム設計(Dayごとのトピック一覧)
  days/
    day01_音とオクターブ/
      script.md          # 台本(音声合成の元テキスト)
      audio.aiff / .m4a  # 生成した音声ファイル
    day02_.../
    ...
```

## 音声生成方法
`tools/build_audio.py` で script.md から audio.m4a を一括生成する(手動でsayを叩かない)。

```
python3 tools/build_audio.py days/day01_音とオクターブ/script.md days/day01_音とオクターブ/audio.m4a
```

- ナレーション: macOSの`say`コマンド、voice=`Kyoko (Enhanced)`、rate=160(標準よりゆっくりめで優しい印象に調整)
  - Siriの声はsay/公開APIから呼び出せない(OS内部に隔離されており回避策なし)ため使用不可
  - Kyoko (Enhanced) はシステム設定からダウンロード済み(2026-08-11)。標準Kyokoより自然な音質
- 実音: サイン波ではなく、macOS内蔵GM音源(`/System/Library/Components/CoreAudio.component/.../gs_instruments.dls`)の
  アコースティックグランドピアノ音色を`tools/piano_tone`(Swiftバイナリ、AVAudioEngineでオフラインレンダリング)で生成
  - `tools/piano_tone.swift` を `swiftc` でコンパイル済み。ソース変更時は再コンパイルが必要:
    `swiftc tools/piano_tone.swift -o tools/piano_tone -framework AVFoundation`
- 台本(script.md)の書式:
  - 空行区切りの段落 = ナレーション(そのまま読ませるため、見出し記号は使わず話し言葉の文章のみ)
  - `[TONE:C4]` や `[TONE:C4,E4,G4]` の行 = その音(単音 or 和音)をゆったり鳴らす(音程・和音の聴き取り用)
  - `[BEAT:C4,E4,G4]` の行 = その音を短く、120bpm相当(0.5秒間隔)のテンポ感で鳴らす
    (拍子・リズムのデモ用。2026-08-15〜。「強、弱」のようなナレーションと同じ体感速度に合わせたい回で使う)
  - `[PAUSE:1.5]` の行 = 指定秒数の無音を挿入する
  - `{{EN:English Term}}` = 直前の専門用語のネイティブ発音を、voice=`Samantha`で音声内に挿入する
    (2026-08-12〜。Webページの台本表示では自動で「(English Term)」という括弧書きに変換される)
    (2026-08-16、Day1〜8にも遡って追加し、全Day(1〜30)で統一済み)
  - 音名や専門用語が初出の際は、読み方が自然になるよう平仮名や読点で調整する

## 発音辞書(漢字の読み間違い対策、2026-08-16〜)
- `build_audio.py` の `PRONUNCIATION_FIXES` と `DEGREE_PATTERN` で、sayが読み間違えやすい
  音楽用語を、音声合成の直前だけ正しい読みのひらがなに変換する(script.md自体やWeb台本表示は漢字のまま)
  - 例: 「2度」→「にど」(「にたび」と誤読されるのを防ぐ)、「属七の和音」→「ぞくしちのわおん」、
    「弾く」→「ひく」、「白鍵」→「はっけん」、「黒鍵」→「こっけん」
  - 「一度」(もう一度、など「1回」の意味)は数字ではなく漢数字で書かれているため、この変換の対象外
  - 「方」は「〜かた」(方法・様子・人を指す敬称、例: 考え方・弾き方・あの方)と「〜ほう」
    (方向・比較・一般名詞、例: 方向・両方・の方が)の2通りの読みがあり、say任せだと頻繁に
    取り違えるため、単語の全体一致で個別に固定している(単独の「方」に対する汎用ルールではない)
  - 2026-08-16、全Day(1〜30)+おまけ回の音声を、この辞書を反映して再生成し、統一済み
- 新しい用語で読み間違いが疑われる場合は、`PRONUNCIATION_FIXES` に追記する。ユーザーから
  具体的な箇所の指摘があれば、それを優先して辞書に追加すること。追加したら該当Dayの
  audio.m4aを再生成し、必要なプレイリスト・feed.xmlも作り直すこと

## クイズへのジャンプ機能(2026-08-16〜)
- `build_audio.py` は、台本内で「聴き取り練習」という文字列を含む行が最初に現れた時点の再生位置(秒)を、
  `audio.marks.json`(`{"quiz": 秒数}`)として、audio.m4aと同じディレクトリに自動出力する
- `build_player_page.py` はこの marks.json を読み、プレーヤーに「クイズへ mm:ss」ボタンを追加する
  (タップすると `audio.currentTime` をその秒数にセットして直接ジャンプする)
- 台本を修正して尺が変わった場合は、`build_audio.py` を再実行すれば marks.json も自動的に更新される

## 聴き取り練習の間の取り方
- 音を鳴らした直後に「正解は〜」と発声すると、考える余裕がなく答え合わせになってしまう
- 聴き取り練習で最後のTONE/BEATの後、正解発表の行の前に `[PAUSE:1.5]` を挿入し、
  1.5秒ほど考える時間を作る(2026-08-15〜。2026-08-16、Day1〜15にも遡って追加し、全Dayで統一済み)

## 英語併記(専門用語)の方針
- 対象は全Day(1〜30)の全ての専門用語(音階名、音程の質、和音の種類、その他の理論用語全般)
  (2026-08-16、Day1〜8にも遡って追加し統一済み。それ以前はDay9以降のみが対象だった)
- 例: 和声的短音階{{EN:Harmonic Minor Scale}}、導音{{EN:Leading Tone}}、調号{{EN:Key Signature}}
- 同じDayの台本内で同じ用語が複数回登場する場合、英語併記は初出の1回のみでよい(毎回付けると冗長)
- 音声では日本語の後にネイティブ発音の英語がそのまま挟まる形になる(カタカナ読みにしない)

## 図解の方針
- 音声(自転車移動中)では図は見せられないが、後から復習する際にWebページで見返すと理解が深まる回には図解を追加する
- 五度圏のように「空間的な配置」で理解した方が早い概念が対象(例: 五度圏、鍵盤上の音の配置など)
- 実装: `tools/build_player_page.py` の `DAYS` 内、該当Dayのエントリに `"diagram"` キーでinline SVGのHTML文字列を追加する(`<figure class="diagram"><svg>...</svg><figcaption>...</figcaption></figure>` の形)
  - 色は `var(--accent)` 等、ページのCSSトークンをSVG内でそのまま使う(テーマ追従)
  - 台本の内容(本文で扱った具体例、悲愴・月光など)と図をひもづけると復習効果が高い
- 図解を追加すべきか毎回迷う場合は、「自転車では説明できない空間的な関係を扱っているか」を基準に判断する

## Web公開(Android等ブラウザで聴けるようにする)
音声はArtifactツールでHTMLページとして公開し、Dayごとに固有URLを発行している(2026-08-11〜)。

- `tools/build_player_page.py` が `days/dayNN_.../audio.m4a` + `script.md` を読み込み、
  音声をdata URIとして埋め込んだ単体HTMLを `web/dayNN.html` に生成する
  - ページ内容: タイトル、この回のポイント(`DAYS`内に手書き)、(あれば)図解、台本のテキスト版(折りたたみ)、前後Dayへのリンク
  - プレーヤーは画面下部に固定表示、「10秒戻る」ボタン付き
  - 新しいDayを追加したら、`build_player_page.py` 内の `DAYS` リストに `num`/`dir`/`title`/`desc`/`points` を追記する
- URLの管理: `web/urls.txt` に `day01=https://...` の形式で確定済みURLを記録している。
  新しいDayを公開する手順:
  1. `python3 tools/build_player_page.py` でHTML生成(この時点では新Dayのリンクはまだ空)
  2. 新DayのHTMLをArtifactツールで公開してURLを取得
  3. `web/urls.txt` に追記
  4. 再度 `build_player_page.py` を実行(前後リンク・目次リンクが埋まる)
  5. 影響を受けた全ページ(新Day、前のDay、index.html)をArtifactツールで再公開
     (同じfile_pathで呼べば同じURLのまま更新される)
- 目次ページ(`web/index.html`)のURLは固定: `https://claude.ai/code/artifact/4617f287-5bd2-480d-8e88-4881cacc35d2`
  - 目次は`PHASES`(curriculum.mdのPhase構成と対応)でDayをグルーピングして見出し付きで表示する(2026-08-16〜)。
    新しいPhaseを追加する場合は `build_player_page.py` の `PHASES` リストにも追記する
- Artifactは既定で非公開。ユーザーはclaude.aiにログインした状態でアクセスする想定
- **2026-08-16〜、Web(Artifact)の再公開は後回しでよい方針に変更。** ユーザーは今後、
  Podcast版を中心に聴取・台本確認する。音声やfeed.xmlを更新した際、Webのdocs/HTMLファイルは
  リポジトリ内(build_player_page.pyの出力)は最新化しておくが、Artifactツールでの実際の
  再公開(13〜32回分の個別publish)は都度は行わず、まとめて依頼された時に対応する

## Podcast配信(2026-08-16〜、現在はこちらが本命)
再生操作(倍速・一時停止・ダウンロード・続き再生)をPodcastアプリに任せられるよう、
Webページ方式と並行してPodcast配信も用意している。ユーザーは主にこちらを使う。

- リポジトリ: https://github.com/gobeetle2026/music-theory-podcast (public、GitHub Pagesで配信)
  - `git init`済みのこのディレクトリ(`音楽理論学習/`)自体がリポジトリのルート
  - GitHub Pages無料枠は公開リポジトリでのみ利用可能なため、公開リポジトリにしている
    (URLを知っていれば誰でも音声・台本にアクセス可能。ユーザーの許可済み、2026-08-16)
  - 認証は `gh auth login` 済み(アカウント: gobeetle2026、AnimeGoと同じアカウント)
- 公開URL: `https://gobeetle2026.github.io/music-theory-podcast/`
  - PodcastフィードURL: `https://gobeetle2026.github.io/music-theory-podcast/feed.xml`
  - カバーアート: `web/cover.png`(1400×1400、`tools/`に生成スクリプトはなく都度PIL手動生成。ヒラギノ角ゴシックで作成)
- `tools/build_podcast_feed.py` が `build_player_page.py` の `DAYS`/`BONUS` を読み込み、
  各Dayのaudio.m4aから `afinfo`(macOS標準)で長さを取得してRSS(iTunes拡張タグ付き)を `feed.xml` に生成する
  - おまけ回は `itunes:episodeType=bonus`、本編は `itunes:episode` にDay番号を設定
  - 実際の配信日ではなく、Day1を起点に1日ずつずらした架空のpubDateを使う(podcastアプリでの並び順を安定させるため)
  - 連続再生プレイリスト(playlists/)はPodcastフィードには含めていない
    (Podcastアプリ自体に連続再生・キュー機能があるため、Web版専用の仕組みとして残す)
- 新しいDay/おまけ回を追加したときの公開手順:
  1. 通常通り `days/dayNN_.../` に script.md・audio.m4a を作成
  2. `build_player_page.py` の `DAYS`(または`BONUS`)に追記(Web版と共通)
  3. `python3 tools/build_podcast_feed.py` でfeed.xml再生成
  4. `git add -A && git commit && git push`
  5. GitHub Pagesの再ビルドを待ち(`gh api repos/gobeetle2026/music-theory-podcast/pages/builds/latest --jq .status`が`built`になるまで)、
     `curl` で新しいエピソードのenclosure URLが200を返すことを確認する
- Podcastアプリでの購読方法: 各アプリの「URLでフォロー/購読」機能に上記フィードURLを貼り付ける
  (Apple Podcasts配信元ディレクトリへの申請はしていない、フィードURL直接指定のみ)

## 連続再生モード(自転車での聞き流し用、2026-08-16〜)
- `tools/build_playlist.py <開始Day> <話数> playlists/playlistNN_dayXX-YY/audio.m4a` で、
  指定した範囲のDayのaudio.m4aを1本につなげた音声を生成する(各Dayの間に「第N回です」という
  短いアナウンスを自動で挿入)
  - 通常回(既定ビットレート、約79kbps)のままだと5話分で16MBの上限を超えるため、
    プレイリストだけビットレートを下げて(40kbps)書き出している。音質は通常回よりやや粗くなる
  - 出力と同じ場所に `audio.marks.json` として各DayのDay開始位置(秒)も書き出す
    (Webページの「Dayへジャンプ」ボタン用。build_audio.pyのquizマークと同じ考え方)
- Web側は `tools/build_player_page.py` の `PLAYLISTS` リストに `id`/`dir`/`title`/`days` を追記する
  (`dir` は `playlists/` 配下のフォルダ名と一致させる)
- 現在、Day1〜30を5話ずつ、playlist01〜playlist06として作成済み。目次の「連続再生」セクションに一覧表示される
- 新しいDayを追加してPhaseが増えた場合、対応するプレイリストも作り直すか、新しいプレイリストを追加すること

## おまけ回(単発のリクエスト曲解説)
- Day1〜30の番号付きカリキュラムとは別に、ユーザーからのリクエストで実在の曲を理論で読み解く
  「おまけ回」を作れる。`tools/build_player_page.py` の `BONUS` リストに `id`/`dir`/`title`/`desc`/`points` を追記する
  (`id` は `bonus01` のように、`web/bonusXX.html` のファイル名と `urls.txt` のキーにそのまま使われる)
- ディレクトリは `days/bonusNN_曲名/` に script.md・audio.m4a を置く(Day用と同じ build_audio.py を使う)
- 目次には「おまけ」セクションとして別枠で表示される(Phaseの一覧とは分離)
- 実在の楽曲を扱うため、Day14・23・30と同様、着手前にWebSearch/WebFetchでキーやコード進行を裏取りすること
  (ギターのコード表は演奏用にキーを変えて書かれていることがあるので、実音のキーを確認すること。
   例: エンドレスレインはハ長調のコードフォームで書かれるが、半音下げチューニングのため実音はシ長調)

## 進捗状況(このセクションはDayを追加するたびに更新する)
- 完了: Day1〜30、全課程完了(音声・Web公開とも完了)。2026-08-16
- おまけ回: bonus01(エンドレスレイン/X JAPAN)、bonus02(耳のリセットドリル)完了・公開済み
- 連続再生: playlist01〜06(Day1-5, 6-10, 11-15, 16-20, 21-25, 26-30)完了・公開済み
- 目次URL(固定): `https://claude.ai/code/artifact/4617f287-5bd2-480d-8e88-4881cacc35d2`
  ここから全Day・おまけ・連続再生すべてに辿れる。個別URLの一覧は `web/urls.txt` が正
- 今後、追加のDayや改修依頼があれば、既存の仕組み(TONE/BEAT/PAUSE/EN併記/発音辞書/クイズジャンプ/
  Phase別目次/おまけ回/連続再生)を踏襲する。curriculum.mdはPhase7(Day29-30)で完結しており、
  本編を延長する場合はcurriculum.mdにも新しいPhase/Dayを追記すること
- 直近の変更: 2026-08-16、GitHub Pages + feed.xmlによるPodcast配信を追加(リポジトリ化、tools/build_podcast_feed.py新規作成)。
  Podcastのdescription/content:encodedに台本全文を表示するよう対応
- 直近の変更: 2026-08-16、連続再生モード(playlist01〜06)を追加
- 直近の変更: 2026-08-16、発音辞書(弾く/白鍵黒鍵/方の文脈別読み)・英語併記(Day1〜8に遡って追加)・
  PAUSE(Day1〜15に遡って追加)を全Day(1〜30)+おまけ回で統一し、全音声・全プレイリスト・feed.xmlを再生成。
  以後、Web(Artifact)の再公開は都度行わず、Podcast版の更新を優先する運用に変更
- ユーザーは絶対音感を持つが、黒鍵比率の高い調(特にロ長調 = シレ#ファ#)を、隣接する
  白鍵の和音(ハ長調)と取り違えやすいと感じている。これは絶対音感保持者に一般的な、
  黒鍵音の誤認しやすさに起因する現象で、感覚の衰えではないと説明済み。bonus02はその
  耳コピー後のリカバリー用に作った。今後、黒鍵の多い調を扱う際はこの文脈を踏まえること

## 台本執筆の方針
- 話し言葉・「です/ます」調で、実際に語りかけるように書く
- 絶対音感を活かし、「今から鳴らすこのドの音を覚えておいてください」のような具体音の参照を積極的に使う
- 抽象的な説明の後は必ず具体例(知っている曲・弾いたことのある曲との関連づけ)を入れる
- 悲愴・月光など本人の演奏経験がある曲を例に使うと理解が早い
- 実音を鳴らす([TONE:...])機会は、理解が深まるとの実感を踏まえ、Day14までの平均(1日あたり約10個)の
  1.5倍程度、1日15〜20個程度を目安にする(2026-08-14〜)。ただし不自然に音を増やすのではなく、
  説明の各ステップに対応する形で自然に増やす
- 実在の楽曲の具体的事実(調・拍子・和音・強弱記号など)を扱う回は、記憶だけに頼らずWebSearch/WebFetchで
  裏取りしてから台本を書く(Day14での方針。ユーザーが実際に演奏した曲を扱うため誤りは避ける)
